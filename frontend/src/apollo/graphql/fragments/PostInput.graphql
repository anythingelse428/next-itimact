fragment PostInput on Post {
    input: {
      title: $title
      content: $content
      status: $status
      tags: $tags
      categories: $categories
      featuredImageUrl: $featuredImageUrl
    }
}
